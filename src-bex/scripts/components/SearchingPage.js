import {Component} from '../components';

export class SearchingPage extends Component {
  constructor(tabManager) {
    super("SearchingPage");
    this.tabManager = tabManager;
  }

  init() {
    super.init();
  }

  destroy() {
    super.destroy();
  }
}
